"""Self-contained single-modality task models (component demonstrators).

Each subpackage trains on data we already have, is independently useful, and produces an
encoder the multimodal fusion model (``poultryai.models.multimodal``) later reuses. See
``docs/ai-model-plan.md`` for how the tasks converge on the thesis contributions.
"""
